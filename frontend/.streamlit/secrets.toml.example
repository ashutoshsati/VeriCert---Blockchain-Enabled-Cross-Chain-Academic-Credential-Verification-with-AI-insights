# Copy to secrets.toml (git-ignored) to avoid typing settings each time. All optional.
VERICERT_API_URL = "http://localhost:3000"
# VERICERT_ADMIN_KEY = "same value as ADMIN_API_KEY in backend/.env"
# VERICERT_ISSUER_NAME = "Example University"
