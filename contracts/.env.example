# Only needed for `npm run deploy:testnets` (the backup to deploying from Remix).
# Copy to contracts/.env. Never commit .env.

AMOY_RPC_URL=https://rpc-amoy.polygon.technology
FUJI_RPC_URL=https://api.avax-test.network/ext/bc/C/rpc

# MetaMask -> Account details -> Show private key. Use a wallet that only holds testnet tokens.
DEPLOYER_PRIVATE_KEY=
